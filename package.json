{
  "name": "academic-assistant",
  "private": true,
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy": "hardhat ignition deploy ignition/modules/AcademicSystem.ts --network localhost",
    "sync-abis": "node scripts/sync-abis.mjs",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-viem": "^3.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "@types/node": "^24.0.0",
    "dotenv": "^16.0.0",
    "hardhat": "^2.28.6",
    "ts-node": "^10.9.0",
    "typescript": "^5.4.0",
    "viem": "^2.0.0"
  }
}
